import 'package:flutter/material.dart';
import 'screens/product_categories_screen.dart';

class ProductCategoriesLayout extends StatelessWidget {
  const ProductCategoriesLayout({Key? key}) : super(key: const Key('categories'));

  @override
  Widget build(BuildContext context) {
    return const ProductCategoriesScreen();
  }
}
